type TodoItemProps = {
    id: string
    title: string
    complete: boolean
}

export default function TodoItem({id, title, complete}: TodoItemProps){
    return (
        <>
            <li>m</li>
        </>
    )
}